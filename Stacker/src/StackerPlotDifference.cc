#include "../interface/Stacker.h"

void Stacker::plotDifference(std::vector<std::string>& argvStr) {
    //std::vector<std::string> newArgvStr;
    //std::vector<std::string>::iterator setIt = argvStr.begin();
//
    //while (stringContainsSubstr(*setIt, ".root")) setIt++;
//
    //std::vector<std::string>::iterator fileStart = setIt;
    //while (! stringContainsSubstr(*fileStart, "-COMP")) fileStart++;
//
    //std::vector<std::string>::iterator fileStop = fileStart;
    //while (stringContainsSubstr(*fileStop, ".root")) fileStop++;
    //
//
    //newArgvStr.insert(newArgvStr.begin(), fileStart, fileStop);
    //newArgvStr.push_back(*setIt);
//
    //for (auto it : newArgvStr) std::cout << it;
    //std::cout << std::endl;

    //Stacker* altStacker = new Stacker(newArgvStr);

    // loop histograms in this stacker and get each time the stack of the other one thanks to the processList stuff
    
    
}
